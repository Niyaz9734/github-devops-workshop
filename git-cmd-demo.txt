     git status
     git status
     rm testing.py 
     git restore testing.py 
     history 
     git status  git status 
     git add demo.txt
     history | head
     history | tail
     git checkout -b feature/issue-id
     git fetch origin
     git rebase origin/main
     git merge --no-ff feature/issue-id
     gh secret set AWS_SECRET_ACCESS_KEY --body "your_key"
     gh run watch
<<<<<<< HEAD
     rm -rf name_of_your_folder
     git log --oneline --graph --decorate
     gh workflow run <file.yml>
     
=======
     git log --oneline --graph --decorate
     gh workflow run <file.yml>
     git fetch origin
     git rebase origin/main
     git diff branchA..branchB (show the diff of what is in branchA that is not in branchB)
     git log branchA..branchB (show the commit in baranchA taht are not in branchB)
     git merge [branch]

    this is new change on this code...Thank-you

